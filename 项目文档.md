# RAG Research Assistant System 项目文档

> 本文档基于当前仓库实现，描述系统功能、整体流程、RAG 策略（解析/中间态/入库/检索）、Agent 的 deep-research 式问答机制与创新点。

## 1. 项目介绍（功能、流程）

### 1.1 系统目标
本项目是一个面向“深度研究/解题/知识问答”的 RAG 系统：
- 支持上传 PDF/MD 等文档
- 将文档解析为带层级结构的“类 OCR”布局信息
- 通过层级 chunking + 向量索引实现语义检索
- 通过 LangChain Agent（工具调用 + 反思式迭代）生成多步推理/计划型回答
- 前端支持 Split-screen（左侧 PDF 预览，右侧 Agent Chat）与流式展示推理过程

### 1.2 架构与目录分工
- 前端（React）：`src/`
  - Upload / Search / AgentWorkspace 三个主要页面（Tab）
  - Agent Chat 以 NDJSON 事件流的方式渲染“思考/工具/观察/回答”
- 后端（FastAPI）：`backend/`
  - `/api/upload`：上传文件并触发入库（embedding + 向量写入）
  - `/api/search`：直接向量检索（简化版语义搜索）
  - `/api/parse`：文档解析预览（优先走增强解析）
  - `/api/agent/*`：Agent 问答（同步/流式）
- RAG Core（知识库 + chunker + 向量库 + Agent）：`rag single/`
  - `knowledge_base/`：PDFProcessor、层级 chunker、FAISS 向量库存取
  - `agent.py`：LangChain Tools Agent + 流式事件转发

入口与关键文件：
- 后端入口：`backend/main.py`
- 上传/索引：`backend/api/routes/upload.py` → `rag single/knowledge_base/kb.py`
- 增强解析预览：`backend/api/routes/parse.py` → `rag single/knowledge_base/enhanced_system.py`（PDFProcessor + DotsHierarchicalChunker）
- 语义搜索：`backend/api/routes/search.py` → `EnhancedVectorStore.similarity_search_with_score`
- Agent API：`backend/api/routes/agent.py` → `rag single/agent.py`
- 前端 Agent 工作台：`src/components/AgentWorkspace.js`、`src/components/AgentChat.js`

### 1.3 端到端流程（用户视角）

#### 流程 A：上传并入库（Indexing）
1. 前端上传文件（`src/components/FileUpload.js`）调用 `/api/upload`
2. 后端保存到 `rag single/uploads/`（后端把该目录以 `/uploads` 静态挂载，便于预览）
3. 后端调用 KnowledgeBase.add_document → PDFProcessor.process（PDF→结构化 JSON）→ DotsHierarchicalChunker.chunk（层级 chunk）→ EnhancedVectorStore.add_chunks（embedding + FAISS 写入）

#### 流程 B：语义搜索（简化检索）
1. 前端 Search（`src/components/Search.js`）调用 `/api/search`
2. 后端调用 `EnhancedVectorStore.similarity_search_with_score` 返回内容与分数

#### 流程 C：Agent 深度问答（deep research 风格）
1. 前端 AgentChat（`src/components/AgentChat.js`）调用 `/api/agent/chat_stream`
2. 后端 `Agent.run_stream` 将 LangChain astream_events 转成 NDJSON 事件：
   - thought / thought_chunk：思考过程
   - observation：工具返回
   - answer_chunk：回答 token
   - final_answer：最终答案
3. 前端实时渲染 reasoning（Accordion）与回答正文

---

## 2. RAG 策略

本项目的 RAG 不是“纯按字数切块”，核心在于：
- **类 OCR 的结构化解析**（把 PDF 页面解析为“布局块/标题层级/类别”）
- **层级 chunking**（把标题树作为上下文路径）
- **上下文增强向量化**（embedding 输入包含：类别、层级上下文、caption、正文）

### 2.1 如何对 PDF 进行类 OCR 解析
实现位置：`rag single/knowledge_base/enhanced_system.py` → `class PDFProcessor`

关键思路（用版式信息模拟 OCR 输出）：
1. **优先使用 PyMuPDF(fitz)** 获取页面 blocks/lines/spans：
   - span 内包含文本与字体大小（size）与 bbox
   - 通过统计全文字体大小直方图（font histogram）推断标题级别
2. **字体大小启发式分类**：
   - 最大字体 → `Title`
   - 次大/第三大字体 → `Section-header`（分别前缀 `#` 或 `##`）
   - 其余 → `Text`
3. **生成“页面布局 JSON”**：每页输出 `page_no` + `full_layout_info` 列表，列表中每个 box 至少包含：
   - `text`：文本（标题会被加上 Markdown 风格的 `#` 前缀）
   - `category`：Title / Section-header / Text / Table / ...
   - `page_no`：页码
   - `bbox`：方框坐标（便于 debug 与未来扩展）
4. **回退逻辑**：如果没有 fitz，则使用 `pypdf` 提取纯文本并做简单启发式（全大写/编号开头的短行→标题）。

> 这个解析过程是“类 OCR”：并不做真实图片 OCR，但保留了 OCR pipeline 常见的中间产物（layout boxes + category + bbox）。

### 2.2 解析之后如何组织成 JSON 中间态
中间态（`json_doc`）结构（简化示例）：

```json
[
  {
    "page_no": 1,
    "full_layout_info": [
      {"text": "Title of Paper", "category": "Title", "page_no": 1, "bbox": [..]},
      {"text": "# 1 Introduction", "category": "Section-header", "page_no": 1, "bbox": [..]},
      {"text": "We propose ...", "category": "Text", "page_no": 1, "bbox": [..]}
    ]
  },
  {
    "page_no": 2,
    "full_layout_info": [
      {"text": "## 1.1 Background", "category": "Section-header", "page_no": 2, "bbox": [..]},
      {"text": "...", "category": "Text", "page_no": 2, "bbox": [..]}
    ]
  }
]
```

这个 JSON 的作用：
- 作为 **解析与 chunking 的解耦层**：chunker 不依赖具体 PDF 库输出格式，只依赖统一 schema
- 便于 debug：PDFProcessor 还会把完整 JSON dump 到 `rag single/debug_output/*_layout.json`

### 2.3 如何 chunking 并将解析后的文档存入数据库

#### 2.3.1 层级 chunking：DotsHierarchicalChunker
实现位置：`rag single/knowledge_base/enhanced_system.py` → `class DotsHierarchicalChunker`

核心机制：
- 扫描所有页面的 `full_layout_info`，为每个 box 分配一个全局递增的 `idx`
- 维护 `heading_by_level: level -> idx`，将标题栈作为“当前上下文”
- 对 `Section-header`：
  - 根据 `#` 数量计算 level（例如 `#`=1，`##`=2）
  - 更新标题栈（更深层标题出现时会清理同级/更深层的旧标题）
  - 建立 parent-child 关系（`children`）
- 对普通 Text/Table/Picture/Formulas：
  - 将当前标题栈转为 `headings: [heading_idx1, heading_idx2, ...]`
  - Table/Picture/Formulas 会尝试吸附相邻的 `Caption`

产物是 `Dict[int, DotsChunk]`：key 为 chunk idx，value 为包含 category/page/headings/caption/children 的结构。

#### 2.3.2 向量库入库：EnhancedVectorStore（FAISS）
实现位置：`rag single/knowledge_base/enhanced_system.py` → `class EnhancedVectorStore`

写入逻辑（关键点是“上下文增强”）：

**数据库里到底存的是什么？**
- 这里的“数据库”指的是：
  - 一个 FAISS 向量索引文件（`*.faiss`）
  - 一个与之配套的元数据 JSON（`*_meta.json`，保存 documents/metadatas/ids）
  - 持久化目录由 `rag single/knowledge_base/kb.py` 决定（英文库默认在系统临时目录 `faiss_db_en/` 下）。

**入库时对什么内容做 embedding？**
- 对每个 `DotsChunk`，系统不会直接对 `chunk.text` 做 embedding，而是先拼装一个“增强上下文字符串” `full_context`，然后对 `full_context` 做 embedding。
- `full_context` 的拼装格式（来自 `EnhancedVectorStore.add_chunks`）：
  - `Category: {chunk.category}`
  - `Hierarchical Context: Heading1 > Heading2 > ...`（把 `chunk.headings` 的 id 反查成标题文本）
  - `Caption: {chunk.caption}`（若存在）
  - `Content: {chunk.text}`（正文）

一个具体例子（伪示例，格式与代码一致）：
```text
Category: Text
Hierarchical Context: Title of Paper > 1 Introduction > 1.2 Method
Caption: Figure 2: Overview
Content: We propose a multi-stage pipeline that...
```
- 同时写入 metadata sidecar（JSON），字段包括：
  - `chunk_id / category / page_no / source`
  - `headings_ids`（标题 id 列表，json 字符串）
  - `headings`（标题文本列表，json 字符串）
  - `original_text`（原始正文）
  - `context_str`（`Heading1 > Heading2` 的拼接）

embedding 与索引：
- embedding 模型：`SentenceTransformer('all-MiniLM-L6-v2')`
- 向量归一化后使用 `faiss.IndexFlatIP`（对单位向量做内积，等价于 cosine 相似度）
- 持久化位置（英文库）：系统临时目录下 `faiss_db_en/`（由 `knowledge_base/kb.py` 决定）

更精确地说：
- 每条入库向量：`v_i = normalize( embed(full_context_i) )`
- FAISS 索引存储的就是这些 `v_i`
- 元数据 sidecar 里会保留：
  - `documents[i] = full_context_i`（用于 debug/重建）
  - `metadatas[i]["original_text"] = chunk.text`（用于展示正文）
  - `metadatas[i]["context_str"] = "Heading1 > Heading2"`（用于 UI/Agent 展示层级路径）

触发入库的入口：
- 后端：`backend/api/routes/upload.py`
- 知识库封装：`rag single/knowledge_base/kb.py` → `add_document`

### 2.4 如何进行 retrieval

检索入口与返回形态有两种：

#### A) 简化 search（前端 Search 页）
- 后端：`backend/api/routes/search.py`
- 调用：`EnhancedVectorStore.similarity_search_with_score(query, k=5)`
- 返回：`[{ section, content, score, source }]`

这个路径下“相似度到底算的是什么？”
- query 侧：把用户 `query` 做 embedding 得到 `q = normalize(embed(query))`
- 库侧：与索引中每个 `v_i = normalize(embed(full_context_i))` 计算内积
  $$score_i = q \cdot v_i$$
- 因为 `q` 和 `v_i` 都做了 L2 归一化，所以 $q \cdot v_i$ 等价于 cosine 相似度。

返回的 `content` 是什么？
- `similarity_search_with_score` 返回的 `Document.page_content` 实际来自 metadata 的 `original_text`（也就是 chunk 正文），**不是** `full_context`。
- 也就是说：
  - “用于相似度计算”的文本是 `full_context`
  - “展示给用户”的文本主要是 `original_text`（正文）

#### B) 结构化 retrieval（Agent 使用）
- 知识库：`KnowledgeBase.retrieve_structured(query, k=5)`
- 向量库：`EnhancedVectorStore.retrieve_structured`
- 返回字段（chunk_info）：
  - `rank, id, page_no, category, score`
  - `headings`（标题文本列表）
  - `headings_ids`（标题 id 列表）
  - `caption, children, source`
  - `text`（正文）
  - `context`（标题路径字符串）

> 这套 structured retrieval 的意义：让 Agent 能“看到层级结构”，不仅仅是看到片段文本。

结构化 retrieval 的相似度计算同上：
- 相似度仍然是 `embed(query)` 与 `embed(full_context)` 的 cosine
- 只是返回结构额外包含 `headings/context/page_no/category/caption`，便于 Agent 做“定位章节→判断相关性→决定是否改写 query”

---

## 3. AGENT

### 3.1 如何实现类 deep research 的问题回答

#### 3.1.1 Agent 架构
实现位置：`rag single/agent.py`

- 使用 LangChain `create_openai_tools_agent` + `AgentExecutor`
- LLM：`ChatOpenAI`（默认 `qwen-max`，走 DashScope OpenAI-compatible 接口；后端可通过 `.env` 的 `LLM_MODEL/LLM_BASE_URL` 覆盖）
- 工具：`search_knowledge(query: str)`
  - 内部调用 `KnowledgeBase.retrieve_structured`
  - 把结构化结果格式化为可读文本（包含 rank/page/category/score/headings/text/context）

#### 3.1.2 Prompt（为何像 deep research）
`Agent._setup_planning_agent()` 中 system prompt 核心约束：
- 你是 solution planning expert
- 遇到未知概念必须调用 `search_knowledge`
- 如果没结果要改写 query 继续搜索（至少搜索一次）
- 根据工具返回迭代生成计划/推理步骤
- 结尾要求把答案再重复一次，并以 `Final Answer:` 开头（用于区分最终答案段）

这会驱动出类似 deep research 的行为模式：
1) 先拆问题 → 2) 检索支撑知识 → 3) 合成步骤/计划 → 4) 不足则再检索 → 5) 输出最终可执行方案。

#### 3.1.3 工具与事件流（前后端如何把推理可视化）
- 后端流式接口：`backend/api/routes/agent.py` → `/api/agent/chat_stream`
  - 调用 `Agent.run_stream()`
  - 将事件以 `application/x-ndjson` 输出（每行一个 JSON）
- 前端渲染：`src/components/AgentChat.js`
  - 解析 NDJSON
  - `thought/thought_chunk/observation` → 追加到 reasoning steps（Accordion）
  - `answer_chunk/final_answer` → 追加或替换回答正文

事件类型示例：
```json
{"type":"thought","content":"Using search_knowledge...","tool":"search_knowledge","tool_input":"RGB to CMY"}
{"type":"observation","content":"[Rank 1] ..."}
{"type":"answer_chunk","content":"Step 1: ..."}
{"type":"final_answer","content":"Final Answer: ..."}
```

> 注意：当前 `run_stream` 会尝试把模型输出中的 `Action:` 隐藏，并将 `Final Answer` 作为回答分界。

#### 3.1.4 一个简单例子（从问题到工具调用）
问题：`Prepare RGB(150,20,190) color, k=0.6`

典型 Agent 行为（概念流程）：
1. 识别这是“颜色配比/浓度/混合”类问题
2. 形成检索 query（示例）：
   - `RGB color preparation`
   - `RGB to CMY`
   - `mixing ratio calculation`
3. 调用 `search_knowledge(query)` 获取公式/方法
4. 把检索到的方法组织成可执行的分步计划
5. 输出 `Final Answer:` 段作为最终方案

### 3.2 过程中如何形成 query 来进行 retrieval

本项目没有手写 query-rewriter，而是通过 Prompt 让 LLM 自主构造 query：
- Prompt 明确要求：未知概念 → 必须检索；没结果 → 改写 query 再检索
- 工具只有一个 `search_knowledge(query)`，因此 LLM 的“研究路径”会自然体现为：
  - 把大问题拆成子问题
  - 子问题用关键词/术语表达为 query
  - 对不同子问题多轮检索

在实现层面：
- query 字符串直接传入 `KnowledgeBase.retrieve_structured`
- 检索结果返回时包含 `headings/context`，LLM 可以据此确认命中章节是否正确，并决定是否继续改写 query

---

## 4. 本项目的创新点

1. **类 OCR 的 PDF 解析策略**：使用 PyMuPDF 的块/行/span 与字体大小直方图，重建“标题/正文”的结构化布局中间态，而不是纯提取一坨文本。
2. **JSON 中间态解耦**：`PDFProcessor → json_doc → DotsHierarchicalChunker` 的管线让解析与 chunking 可独立替换、易 debug、易扩展（也可接入真实 OCR）。
3. **层级 chunking + 结构化检索返回**：不仅存 chunk，还存标题树（headings ids + resolved headings text + context_str），Agent 检索时能“看见”层级上下文。
4. **上下文增强向量化**：embedding 输入不是纯 chunk，而是 `Category + Hierarchical Context + Caption + Content`，显著提升短文本/歧义片段的可检索性。
5. **端到端推理可视化**：后端将 LangChain 事件流转为 NDJSON，前端以 Accordion 展示推理路径与工具观察结果，形成可解释的 deep research 体验。

---

## 附：已知限制/注意事项（基于当前实现）
- `knowledge_base/kb.py` 中 FAISS 持久化路径位于系统临时目录（tempdir），在某些环境清理临时目录可能导致索引丢失。
- embedding 模型 `all-MiniLM-L6-v2` 需要首次下载；国内环境需要配置 `HF_ENDPOINT`（后端已在 `backend/main.py` 设置镜像）。
- Agent 的 `tools_schema.json` 在当前仓库结构中未找到（Agent 代码仍会尝试加载，但逻辑上并不影响工具调用；实际工具列表是代码内 `search_knowledge`）。
