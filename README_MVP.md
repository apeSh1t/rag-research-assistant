# RAG Research Assistant - MVP 1.0 🚀

一个基于 RAG (Retrieval-Augmented Generation) 的智能研究助手系统，支持文档上传、智能检索和 AI 问答。

## ✨ MVP 1.0 功能

- ✅ **文档上传** - 支持 PDF 文档上传和向量化
- ✅ **基础搜索** - 基于 ChromaDB 的语义检索
- ✅ **智能问答** - 集成 Agent 系统，支持多步推理
- ✅ **对话管理** - 支持多轮对话上下文
- ✅ **现代UI** - Material-UI 界面，标签页切换

## 📋 环境要求

### 必需
- **Python**: 3.9+ 
- **Node.js**: 18+
- **npm**: 9+

### API 密钥（必须提供其中之一）
- **阿里云通义千问** (推荐): [获取 API Key](https://dashscope.aliyun.com/)
- **OpenAI**: [获取 API Key](https://platform.openai.com/)

---

## 🚀 快速开始

### 方法 1: 使用自动化脚本（推荐）

#### 步骤 1: 配置环境
```cmd
setup.bat
```
这将自动安装所有依赖并创建配置文件。

#### 步骤 2: 配置 API Key

编辑 `backend\.env` 文件，填入你的 API Key：

```env
# 使用阿里云通义千问（推荐）
DASHSCOPE_API_KEY=sk-your-api-key-here
LLM_MODEL=qwen-max

# 或使用 OpenAI
# OPENAI_API_KEY=sk-your-api-key-here
# LLM_MODEL=gpt-3.5-turbo
```

#### 步骤 3: 启动服务
```cmd
start.bat
```

系统将自动启动：
- 后端服务: http://localhost:8000
- 前端界面: http://localhost:3000

---

### 方法 2: 手动启动

#### 1. 安装前端依赖
```bash
npm install
```

#### 2. 安装后端依赖
```bash
cd backend
pip install -r requirements.txt
cd ..
```

#### 3. 配置环境变量
```bash
# 复制配置文件
copy backend\.env.example backend\.env

# 编辑 backend\.env，填入 API Key
notepad backend\.env
```

#### 4. 启动后端（终端 1）
```bash
cd backend
python main.py
```

#### 5. 启动前端（终端 2）
```bash
npm start
```

---

## 📖 使用指南

### 1. 文档上传
1. 进入 "📤 文档上传" 标签页
2. 拖拽或点击上传 PDF 文档
3. 系统自动提取文本并向量化

### 2. 文档搜索
1. 进入 "🔍 文档搜索" 标签页
2. 输入查询关键词
3. 查看相关文档片段

### 3. 智能问答（核心功能）
1. 进入 "🤖 智能问答" 标签页
2. 输入你的问题
3. AI 助手会：
   - 自动搜索知识库
   - 进行多步推理
   - 提供详细解答

#### 示例问题：
- "解释一下 Transformer 架构的工作原理"
- "RAG 系统的核心组件有哪些？"
- "如何提高检索质量？"

---

## 🎯 API 文档

启动后端后，访问：
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### 核心 API 端点

```
POST /api/upload          # 上传文档
POST /api/search          # 搜索文档
POST /api/agent/chat      # 智能问答
GET  /api/agent/status    # Agent 状态
GET  /health              # 健康检查
```

---

## 🛠️ 技术栈

### 后端
- **FastAPI** - Web 框架
- **LangChain** - LLM 编排
- **ChromaDB** - 向量数据库
- **BGE Embeddings** - 文本嵌入模型

### 前端
- **React 19** - UI 框架
- **Material-UI** - 组件库
- **Fetch API** - HTTP 客户端

### AI 模型
- **通义千问 (Qwen)** / **OpenAI GPT** - 大语言模型
- **BAAI/bge-base-en-v1.5** - 嵌入模型

---

## ⚠️ 常见问题

### 1. "未配置 API Key" 错误
**解决方案**: 编辑 `backend\.env`，填入正确的 API Key

### 2. "Agent 模块未加载" 错误
**解决方案**: 确保 `rag single/` 目录存在且完整

### 3. "向量库未找到" 错误
**解决方案**: 运行以下命令构建索引
```bash
cd "rag single"
python knowledge_base/build_index_en.py
```

### 4. 前端无法连接后端
**解决方案**: 
- 检查后端是否运行在 8000 端口
- 检查 CORS 配置

### 5. 依赖安装失败
**解决方案**: 使用国内镜像
```bash
# pip
pip install -r backend/requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# npm
npm install --registry=https://registry.npmmirror.com
```

---

## 📂 项目结构

```
rag-research-system/
├── backend/                 # 后端服务
│   ├── main.py             # FastAPI 入口
│   ├── api/routes/         # API 路由
│   │   ├── upload.py
│   │   ├── search.py
│   │   └── agent.py        # 🆕 智能问答 API
│   ├── requirements.txt
│   └── .env                # 配置文件
├── src/                    # 前端代码
│   ├── App.js              # 🆕 主界面（标签页）
│   ├── components/
│   │   ├── FileUpload.js
│   │   ├── Search.js
│   │   └── AgentChat.js    # 🆕 智能问答组件
│   └── config.js
├── rag single/             # RAG 核心模块
│   ├── agent.py            # Agent 系统
│   ├── knowledge_base/     # 知识库
│   └── chroma_db_en/       # 向量数据库
├── setup.bat               # 🆕 环境配置脚本
├── start.bat               # 🆕 快速启动脚本
└── README_MVP.md           # 本文档
```

---

## 🎨 MVP 1.0 vs 完整版对比

| 功能 | MVP 1.0 | 完整版（规划中） |
|-----|---------|--------------|
| 文档上传 | ✅ PDF | ✅ PDF/Word/TXT/MD |
| 基础搜索 | ✅ | ✅ |
| 智能问答 | ✅ | ✅ |
| 多轮对话 | ✅ | ✅ |
| 引用溯源 | ❌ | ✅ |
| 文档管理 | ❌ | ✅ |
| 用户认证 | ❌ | ✅ |
| 搜索历史 | ❌ | ✅ |
| 导出功能 | ❌ | ✅ |

---

## 🚀 后续计划

### Phase 2 (2周)
- [ ] 引用溯源功能
- [ ] 文档结构化解析
- [ ] 搜索结果优化

### Phase 3 (3周)
- [ ] 用户认证系统
- [ ] 文档管理界面
- [ ] 高级筛选功能

### Phase 4 (2周)
- [ ] 性能优化
- [ ] Docker 部署
- [ ] 生产环境配置

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 License

MIT License

---

## 🙏 致谢

- LangChain 社区
- ChromaDB 团队
- 阿里云通义千问团队

---

**需要帮助？** 查看 [常见问题](#-常见问题) 或提交 Issue

**开始使用** → 运行 `setup.bat` 然后 `start.bat` 🎉
